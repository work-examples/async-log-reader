#include "CFnMatch.h"


bool CFnMatch::SetFilter(const char* filter)
{
    if (filter == nullptr)
    {
        return false;
    }

    // TODO: reallocate + copy + compress
    // TODO: maybe parse to some tree????????

    return false;
}


bool CFnMatch::CheckMatch(const char* str, const size_t strLength)
{
    // TODO: IMPLEMENT!!!!!!!!!!!!!!!
    return false;
}
